[
  {
   "cmd": "sudo npm install -g @subql/cli",
   "note": "install subquery cli globally"
  },
  {
   "cmd": "subql -v",
   "note": "get cli version"
  },
  {
   "cmd": "sudo npm install -g @subql/node",
   "note": "install subquery indexer node globally"
  },
  {
   "cmd": "subql-node --version",
   "note": "get indexing node version"
  },
  {
   "cmd": "sudo npm install -g @subql/query",
   "note": "install subquery query service globally"
  },
  {
   "cmd": "subql-query --version",
   "note": "get query version"
  },
  {
   "cmd": "subql init <projectname>",
   "note": "initialise starter project"
  },
  {
   "cmd": "npm install",
   "note": "install dependencies"
  },
  {
   "cmd": "npm run-script codegen",
   "note": "generate types"
  },
  {
   "cmd": "npm run-script build",
   "note": "build project"
  },
  {
   "cmd": "subquery -f <projectname>",
   "note": "start indexer"
  }
]
