[
  {
   "cmd": "sudo apt install postgresql",
   "note": "install postgresql"
  },
  {
   "cmd": "psql --version",
   "note": "get version"
  },
  {
   "cmd": "sudo systemctl status 'postgresql*'",
   "note": "get status"
  },
  {
   "cmd": "sudo systemctl start postgresql",
   "note": "start postgresql"
  },
  {
   "cmd": "sudo systemctl stop postgresql",
   "note": "stop postgresql"
  },
  {
   "cmd": "sudo systemctl restart postgresql",
   "note": "restart postgresql"
  },
  {
   "cmd": "\q",
   "note": "quit"
  }
]
