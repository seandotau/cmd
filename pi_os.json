[
  {
   "cmd": "ssh pi@raspberrypi.local",
   "note": "ssh"
  },
  {
   "cmd": "curl -OJL <git_raw_file_url>",
   "note": "download file from github"
  },
  {
   "cmd": "chmod +x <filename>",
   "note": "make file executable"
  },
  {
   "cmd": "uname -a",
   "note": "UNIX name -all"
  },
  {
   "cmd": "df -h",
   "note": "disk free -human readable"
  },
  {
   "cmd": "cat /etc/wpa_supplicant/wpa_supplicant.conf",
   "note": "view WiFi details"
  },
  {
    "cmd": "cat /etc/os-release",
    "note": "OS version"
  },
  {
    "cmd": "sudo apt update",
    "note": "update os"
  },
  {
    "cmd": "sudo apt upgrade",
    "note": "upgrade os"
  },
  {
    "cmd": "env | sort | head -n 5",
    "note": "sort & view top 5 environment variables "
  },
  {
    "cmd": "pip list",
    "note": "list python packages"
  },
  {
    "cmd": ". ~/.bash_profile",
    "note": "update bash_profile" 
  },
  {
    "cmd": ".scp <name> pi@<ip>:<venv_location>",
    "note": "copy file to pi" 
  }
]
